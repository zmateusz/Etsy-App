<%= form_for(@category) do |f| %>

  <div class="name">
  <%= f.text_field :name %>
  </div>
  <div class="thumburl">
  <%= f.url_field :thumburl %>
  </div>
  <div class="actions">
  <%= f.submit %>
  </div>
<% end %>
